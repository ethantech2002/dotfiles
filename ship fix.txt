```bash
# ~/.config/starship.toml

# Inserts a blank line between shell prompts
add_newline = true

# Change the default prompt format
format = "[🌌 {279}]{symbol} {238}{character}"

# Change the default prompt characters
[character]
success_symbol = "[🏆{238}"
error_symbol = "[❌{238}"

# Shows the username
[username]
symbol = "white bold"
style_user = "white bold"
style_root = "black bold"
format = "[$user]($style)"
disabled = false
show_always = true

# Shows the directory
[directory]
truncation_length = 3
truncation_symbol = "…"
home_symbol = "🏠"
read_only_style = "197"
read_only = "🔒"
format = "[$path]($style)[$read_only]($read_only_style)"

# Shows the git branch
[git_branch]
symbol = " on [$symbol$branch]($style)"
format = "[$symbol$branch]($style)"
truncation_length = 4
truncation_symbol = "…"
style = "bold green"

# Shows the git status
[git_status]
format = "[$status$ahead_behind]($style)"
conflicted = "🛑"
untracked = "🤷"
ahead = "↑${count}"
diverged = "↯"
behind = "↓${count}"
staged = "✚${count}"
renamed = "🔄${count}"
deleted = "🗑️${count}"

# Shows the terraform
[terraform]
format = " via [$terraform version]($style)[$workspace]($style)"

# Shows the vagrant
[vagrant]
format = " via [$vagrant version]($style)"

# Shows the docker context
[docker_context]
format = "[$context](bold blue)"
```
```ini
[terraform]
format = "via  #{terraform $version}#{style} → #{workspace}#{style}"

[vagrant]
format = "via  #{vagrant $version}#{style}"

[docker_context]
format = "via  #{context}#{bold blue}"

[helm]
format = "via  #{context}#{bold purple}"

[python]
symbol = "🐍"
python_binary = "python3"

[nodes]
format = "via  #{version}#{style}"
disabled = true

[ruby]
format = "via  #{version}#{style}"

[kubernetes]
format = "on #{context}#{namespace}#{bold purple} "
disabled = false
kubernetes.context_aliases =
  "clcreative-k8s-staging" = "cl-k8s-staging"
  "clcreative-k8s-production" = "cl-k8s-prod"
```